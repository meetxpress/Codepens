function lol() {
	if((username.value.length == "") || (password.value.length == "")){
		alert("One of the field is empty. Please enter value in both the fields.");	
	}else{
        alert("Incorrect Password. Please stop entering your ex's name as a password.");		
    }		
}
